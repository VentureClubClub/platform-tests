{
  "name": "platform-tests",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.2.0",
    "@testing-library/user-event": "^13.5.0",
    "@vendia/serverless-express": "^4.8.0",
    "postgraphile": "^4.12.9",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "netlify-cli": "^10.3.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "env": "env",
    "build": "echo \"no react please for now\" || npm run build:client",
    "build:client": "react-scripts build",
    "build:getGreeting": "cd functions/get-greeting && make build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "postgraphile": "postgraphile --connection \"postgres://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:$POSTGRES_PORT/yugabyte?ssl=true&sslmode=verify-ca&sslrootcert=$(pwd)/root.crt\" --schema public --watch --enhance-graphiql"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
